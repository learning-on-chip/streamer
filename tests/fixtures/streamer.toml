[traffic]
path = "google.sqlite3"
query = """
SELECT 1e-6 * `time` FROM `job_events`
WHERE `time` IS NOT 0 AND `event type` IS 0
ORDER BY `time` ASC;
"""

[[power.sources]]
name = "Black–Scholes"
path = "blackscholes.sqlite3"
